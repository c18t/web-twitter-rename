---
name: Docker Image CI

on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+*'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: Login to GitHub Packages Registry
        run: docker login -u $GITHUB_ACTOR -p ${{ secrets.GITHUB_TOKEN }} docker.pkg.github.com
      - name: Make image name and version
        run: |
          REPOS_L=${GITHUB_REPOSITORY,,}
          IMAGE_NAME=docker.pkg.github.com/${REPOS_L}/${REPOS_L#*/}
          IMAGE_VERSION=${GITHUB_REF#ref/tags/v}
          echo ${IMAGE_NAME}:${IMAGE_VERSION} > specific-version.txt
          echo ${IMAGE_NAME}:latest > latest-version.txt
      - name: Build the Docker image
        run: docker build . --tag $(cat specific-version.txt)
      - name: Push the image to GitHub Packages Registry
        run: docker push $(cat specific-version.txt)
      - name: Build the Docker image as latest version
        run: docker build . --tag $(cat latest-version.txt)
      - name: Push the image to GitHub Packages Registry as latest version
        run: docker push $(cat latest-version.txt)
